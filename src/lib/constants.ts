/** Shared color palette used by chart/list components for tool breakdowns. */
export const PALETTE = [
  "var(--amber)",
  "var(--lime)",
  "var(--cyan)",
  "var(--violet)",
  "var(--coral)",
] as const;
