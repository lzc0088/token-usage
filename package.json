{
  "name": "token-usage",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cross-platform menu bar app for tracking AI agent token usage",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "fetch-tokscale": "node scripts/fetch-tokscale.mjs",
    "pretauri": "npm run fetch-tokscale",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-shell": "^2.3.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@tauri-apps/cli": "^2.11.0",
    "svelte": "^5.56.0",
    "svelte-check": "^4.0.0",
    "tslib": "^2.6.0",
    "typescript": "^5.9.0",
    "vite": "^8.1.0"
  }
}
