{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main popover window",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
